1)
  while True:
    	try:
        
            num=int(input("enter the number: "))
            if num%3==0:
                print(f"yes - {num} is divisible by 3")
            else:
                print(f"no - {num} is not divisible by 3")
        
            break
        except:
            print("no data is provided by the user")